{
  "name": "recourt",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "Apache-2.0",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "turbo run build",
    "dev": "turbo run dev --filter=@recourt/external-service --filter=@recourt/extract --filter=@recourt/internal --filter=web",
    "pregen": "turbo run types --filter=@recourt/api --filter=@recourt/external-service --filter=@recourt/extract --filter=@recourt/internal --filter=web && turbo run generate-routes --filter=web",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "node-addon-api": "^8.9.2",
    "node-gyp": "^13.0.1",
    "oxfmt": "^0.63.0",
    "oxlint": "^1.78.0",
    "oxlint-tsgolint": "^7.0.2001",
    "tsx": "^4.23.12",
    "turbo": "^2.10.12",
    "typescript": "^7.0.2",
    "wrangler": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.7.0",
      "onFail": "download"
    }
  }
}
