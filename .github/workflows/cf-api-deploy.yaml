name: Deploy Cloudflare API Worker

on:
  push:
    branches:
      - main
    paths:
      - app/api/**
      - packages/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/cf-worker-deploy.yaml
    with:
      worker: api
    secrets: inherit
